# Dagu configuration — baked into Docker image, data on persistent volume.
# CVE GHSA-6qr9-g2xw-cw92 mitigated: localhost binding + Caddy auth proxy.
host: "127.0.0.1"
port: 8080

paths:
  dags_dir: "/data/dagu/dags"
  log_dir: "/data/dagu/logs"
  data_dir: "/data/dagu"

# No auth on internal port (localhost only). Caddy handles external auth on :23000.
# This matches how AI Maestro worked: no auth on 23001, Caddy auth on 23000.
auth:
  mode: "none"

# Concurrency: allow parallel agent work on performance-4x/8GB.
# Each agent job needs ~600MB RAM + 1 CPU core for tests.
# With 4 dedicated cores and 8GB, 3 concurrent jobs is comfortable.
queues:
  enabled: true
  config:
    - name: "agent-work"
      max_concurrency: 3

default_shell: "/bin/bash"
tz: "America/New_York"
hist_retention_days: 30
